<!DOCTYPE html>
<html>
  <head>
    <title>Tickets</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body>

    <% if user_signed_in? %>
    <p>
       Bonjour, <%= current_user.name %>  
        <%= link_to "Log_out", destroy_user_session_path, 'data-turbo-method': :delete %></p>
     <% end %> 
    <%= yield %>
  </body>
</html>
